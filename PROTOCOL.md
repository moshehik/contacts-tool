# פרוטוקול העברת אנשי קשר — Q7 ←→ Q8 (נבדק בשטח)

תהליך מוכח מקצה לקצה להעברת אנשי קשר ממכשיר QLYX Q7 למכשיר QLYX Q8 כשר
(ללא אינטרנט — כרטיס זיכרון בלבד).
נבדק: ספטמבר 2026, 1193 אנשי קשר, מעבר מלא תקין.

הכלי באתר (https://moshehik.github.io/contacts-tool/) מיישם את הפרוטוקול הזה:
שלבים 1–3 יוצרים את הקובץ, שלב 4 מדריך את הייבוא, שלב 5 מנקה ייצוא Q7.

## שלב 1 · ייצוא מה-Q7

1. ב-Q7: אנשי קשר ▸ אפשרויות ▸ ייצוא / גיבוי לכרטיס זיכרון.
2. מתקבל קובץ VCF (למשל `VCARD_20240101_000021.vcf`) בפורמט 2.1 עם שמות
   מקודדים quoted-printable.
3. שימו לב: הייצוא מוסיף לשמות סיומות מונה (`_1`, `_1_1`, `_2`...) —
   מנקים אותן בשלב 3.

## שלב 2 · הגדרות ה-Q8 לפני הייבוא (קריטי!)

בלי זה הייבוא "רץ" אבל שום איש קשר לא מופיע:

1. אנשי קשר ▸ אפשרויות ▸ הגדרות ▸ **מיקום שמירה מועדף = טלפון** (לא SIM —
   ב-SIM נכנסים כ-250 אנשי קשר בלבד וייבוא גדול נכשל בשקט).
2. באותו תפריט: **תצוגת אנשי קשר = הכל** (אחרת לא רואים מה שיובא לטלפון).
3. מגבלת ה-Q8: עד **3000** אנשי קשר.

## שלב 3 · ניקוי והכנת הקובץ

1. מעלים את קובץ ה-VCF מה-Q7 לשלב 5 באתר ("ניקוי קובץ VCF").
2. הכלי מסיר את סיומות המונה מהשדות FN ו-N (כולל `N:...:;שם;;;`),
   שומר מספרים ופורמט, ומקפל שורות ארוכות לתקן.
3. מורידים את `contacts_Q8_clean.vcf`.
4. מפרט קובץ נדרש ל-Q8 (נבדק):
   - `VERSION:2.1`, שמות `CHARSET=UTF-8;ENCODING=QUOTED-PRINTABLE`
   - `TEL;CELL:` / `TEL;HOME:`, סיומות שורה CRLF, שורות עד ~70 תווים
   - קידוד UTF-8 ללא BOM

## שלב 4 · העתקה לכרטיס

1. מעתיקים **קובץ אחד בלבד** לתיקיית `vCard` בכרטיס (לא `Alarm`!).
2. אם מייבאים קובץ מתוקן על מכשיר עם הנתונים הישנים — קודם מוחקים הכל
   (אנשי קשר ▸ אפשרויות ▸ מחק הכל) כדי למנוע כפילויות.

## שלב 5 · ייבוא ל-Q8 ואימות

1. מכניסים את הכרטיס, **מכבים ומדליקים** את המכשיר.
2. אנשי קשר ▸ אפשרויות ▸ **ייבוא / שחזור מכרטיס זיכרון** (לא פתיחה דרך מנהל קבצים!).
3. בוחרים את הקובץ, מסמנים הכל, מחכים לסיום (קובץ גדול: דקה-שתיים).
4. אימות: משווים את מספר אנשי הקשר במכשיר למספר שהכלי דיווח.

## טבלת תקלות (נאסף מהשטח)

| תסמין | סיבה | פתרון |
|---|---|---|
| עובר רק איש קשר 1 | פתיחה דרך מנהל הקבצים במקום תפריט אנשי הקשר | יבוא דרך אנשי קשר ▸ יבוא מכרטיס |
| "מייבא" ושום דבר לא מופיע | מיקום שמירה = SIM או תצוגה מסוננת | שלב 2: טלפון + תצוגת הכל + הפעלה מחדש |
| שום קובץ לא נמצא | הקובץ לא בתיקיית `vCard` | להעתיק ל-`vCard`, לא ל-`Alarm` |
| לכל שם נוסף `_1` / `_1_1` | מונה כפילויות של ייצוא Q7 | ניקוי בשלב 5 באתר |
| כפילויות אחרי יבוא מתוקן | הישן לא נמחק | מחק הכל לפני הייבוא המתוקן |
